// stubs/mathjax-version.js
// CSP-safe stub for MathJax's components/version.js.
// The real module reads the version from package.json using require() in Node.
// We don't need that in the extension, so we export a fixed string.

export const VERSION = 'custom-build';
export default VERSION;
