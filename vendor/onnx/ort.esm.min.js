// Re-export the ORT instance from Transformers if present.
import { env as _tEnv } from '../transformers/transformers.esm.js';
const ort = _tEnv?.backends?.onnx;
if (!ort) throw new Error('ORT backend not available');
export default ort;
export const InferenceSession = ort.InferenceSession;
export const Tensor = ort.Tensor;
export const env = ort.env ?? {};
